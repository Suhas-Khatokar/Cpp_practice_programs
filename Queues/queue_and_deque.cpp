#include<iostream>
#include<queue>
#include<deque>
using namespace std;

//Deque is a double sided queue

int main() {
    queue<int> q; //Same push, pop, front, empty func
    deque<int> dq; //push_front, push_back, pop_front, pop_back, empty, 
    return 0;
}